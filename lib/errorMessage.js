const notFound = 'not found'
const unauthorized = 'unauthorized'
const validationError = 'ValidationError'
const error = 'error'

module.exports = {
  notFound,
  unauthorized,
  validationError,
  error,
}
